# Project-scoped Codex configuration.
#
# Codex loads project settings for trusted projects. Keep personal provider
# and authentication settings in your user configuration.

# Allow up to 64 KiB of project instructions. The repo's stricter 28 KiB
# ancestor-chain budget is enforced by `npm run validate:agents`.
# This cap is not a size target; keep task-specific detail in linked references.
project_doc_max_bytes = 65536
