import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as Stories from "./audio-player.stories";

<Meta of={Stories} />

# Audio Player

[Source code](https://github.com/carbon-design-system/carbon-ai-chat/tree/main/packages/ai-chat-components/src/components/audio-player) &nbsp;|&nbsp; [Feedback](https://github.com/carbon-design-system/carbon-ai-chat/issues)

## Overview

Use `cds-aichat-audio-player` to play audio from SoundCloud or native audio files behind a single, accessible interface.

<Canvas of={Stories.Default} />

## Key Features

- **Multi-source support**: Automatically detects and handles SoundCloud and native audio files
- **Accessibility**: Built-in keyboard navigation and ARIA labels
- **Event handling**: Custom events for play, pause, ready, and error states

## Supported Audio Sources

The component automatically detects and handles:

- **SoundCloud**: `https://soundcloud.com/...`
- **Native files**: Direct links to `.mp3`, `.wav`, `.oga`, `.m4a` files

## Events

The component fires the following custom events:

```javascript
const player = document.querySelector("cds-aichat-audio-player");

player.addEventListener("cds-aichat-audio-player-ready", (event) => {
  console.log("Audio is ready to play");
});

player.addEventListener("cds-aichat-audio-player-error", (event) => {
  console.error("Audio error:", event.detail);
});
```

## Component API

<ArgTypes of="cds-aichat-audio-player" />

## JS (via import)

```javascript
import "@carbon/ai-chat-components/es/components/audio-player/index.js";
```

## Feedback

Open an issue at https://github.com/carbon-design-system/carbon-ai-chat/issues to provide feedback or request changes.
