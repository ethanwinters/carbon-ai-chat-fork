import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as Stories from "./markdown-react.stories.jsx";

<Meta of={Stories} />

# Markdown

[Source code](https://github.com/carbon-design-system/carbon-ai-chat/tree/main/packages/ai-chat-components/src/components/markdown) &nbsp;|&nbsp; [Feedback](https://github.com/carbon-design-system/carbon-ai-chat/issues)

## Overview

Use `<Markdown>` to render AI responses with a predictable set of Markdown features plus optional HTML sanitization, including syntax highlighting, interactive tables, and streaming updates.

<Canvas of={Stories.Default} />

### Streaming

Renders progressively as chunks of Markdown arrive, so headings, lists, and code blocks resolve incrementally instead of waiting for the full response.

<Canvas of={Stories.Streaming} />

### With HTML sanitization

With `sanitizeHTML` enabled, dangerous HTML like `<script>` tags and `onclick` attributes is removed while safe HTML is preserved.

<Canvas of={Stories.WithHTMLSanitization} />

### With HTML removal

With `removeHTML` enabled, all HTML tags are stripped, leaving only plain text and Markdown.

<Canvas of={Stories.WithHTMLRemoval} />

### With a markdown-it plugin

Passing a `markdownItPlugins` array extends rendering with custom rules — here, `@vscode/markdown-it-katex` renders LaTeX math into a light-DOM slot.

<Canvas of={Stories.WithMarkdownItPlugin} />

### With a table override

Registering a custom `table` renderer replaces the default `cds-aichat-table` output with a Carbon `Table`, and keeps it mounted while content streams in.

<Canvas of={Stories.WithTableOverride} />

## Usage

Here's a quick example to get you started using the `markdown` prop (recommended):

```jsx
import Markdown from "@carbon/ai-chat-components/es/react/markdown.js";

function MarkdownExample({ content }) {
  return <Markdown codeSnippetHighlight markdown={content} />;
}
```

You can also provide Markdown via children, though the `markdown` prop is recommended for better performance, especially when streaming content:

```jsx
import Markdown from "@carbon/ai-chat-components/es/react/markdown.js";

function MarkdownExample({ content }) {
  return <Markdown codeSnippetHighlight>{content}</Markdown>;
}
```

The `markdown` prop is more efficient than children because it bypasses the slot change detection mechanism. This matters most when streaming Markdown content, since it reduces unnecessary DOM operations and improves rendering performance.

For streaming AI responses, append chunks to the `markdown` prop:

```jsx
function StreamingMarkdown({ generator }) {
  const [content, setContent] = React.useState("");
  const [streaming, setStreaming] = React.useState(true);

  React.useEffect(() => {
    let active = true;
    (async () => {
      for await (const chunk of generator) {
        if (!active) break;
        setContent((prev) => prev + chunk);
      }
      setStreaming(false);
    })();
    return () => {
      active = false;
    };
  }, [generator]);

  return <Markdown streaming={streaming} markdown={content} />;
}
```

## Markdown support

The component implements [GitHub Flavored Markdown (GFM)](https://github.github.com/gfm/) with additional custom extensions. It uses [markdown-it](https://github.com/markdown-it/markdown-it) with the CommonMark preset and includes:

**GFM features:**

- Tables with column alignment
- Strikethrough text (`~~text~~`)
- Automatic URL linking
- Line breaks

**Custom extensions:**

- Highlight syntax (`==text==`) with nested formatting support
- Attribute syntax (`{{attribute=value}}`) for HTML attributes

### Text formatting

- **Bold** text with `**bold**` or `__bold__`
- _Italic_ text with `*italic*` or `_italic_`
- `Inline code` with backticks
- ~~Strikethrough~~ with `~~text~~`
- ==Highlighted text== with `==text==` (renders as a `<mark>` element)
- Nesting, such as `==**bold highlight**==`, renders bold text with highlighting

### Headers

All six levels of headers are supported (`#` through `######`).

### Lists

Both ordered and unordered lists support nested items.

### Links and images

Standard Markdown links `[text](url)` and images `![alt](src)`.

### Blockquotes

> Blockquotes with `>` prefix

### Code blocks

Fenced code blocks with language specification:

```javascript
console.log("Hello, world!");
```

### Tables

Markdown tables with alignment support:

```markdown
| Column 1 | Column 2 |
| -------- | -------- |
| Value 1  | Value 2  |
```

### Attribute syntax

Add HTML attributes to Markdown elements using `{{attribute=value}}` syntax. Delimiters are `{{` and `}}`; allowed attributes are `target`, `rel`, `class`, and `id`.

```markdown
[External link](https://example.com){{target=_blank rel=noopener}}

# Heading with ID {{id=custom-heading}}

Paragraph with custom class {{class=custom-style}}
```

Only the specified attributes (`target`, `rel`, `class`, `id`) are allowed.

## HTML content

Set `sanitizeHTML` to remove potentially dangerous elements and attributes while preserving safe HTML formatting. Set `removeHTML` to strip all HTML tags from the content, leaving only plain text and Markdown.

## Customizing UI text

All text strings used in embedded components can be customized:

**Code snippet strings:**

- `codeSnippetCopyButtonTooltipContent`: tooltip for the copy button (default: "Copy code")
- `codeSnippetShowMoreText`: text for the expand button (default: "Show more")
- `codeSnippetShowLessText`: text for the collapse button (default: "Show less")
- `codeSnippetAriaLabelReadOnly`: aria-label for read-only code snippets (default: "Code snippet")
- `codeSnippetAriaLabelEditable`: aria-label for editable code snippets (default: "Code editor")

**Table strings:**

- `tableFilterPlaceholderText`: placeholder for the table filter (default: "Filter table...")
- `tablePreviousPageText`: previous page button text (default: "Previous page")
- `tableNextPageText`: next page button text (default: "Next page")
- `tableItemsPerPageText`: items per page label (default: "Items per page:")
- `tableDownloadLabelText`: label for downloading table data as CSV (default: "Download table data")
- `tableLocale`: locale for number formatting (default: "en")

## Component API

<ArgTypes of={Stories.Default} />

## Feedback

Open an issue at https://github.com/carbon-design-system/carbon-ai-chat/issues to provide feedback or request changes.
