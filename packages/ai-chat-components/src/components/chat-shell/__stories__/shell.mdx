import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as ChatShellStories from "./shell.stories";

<Meta of={ChatShellStories} />

# Chat shell

[Source code](https://github.com/carbon-design-system/carbon-ai-chat/tree/main/packages/ai-chat-components/src/components/chat-shell) &nbsp;|&nbsp; [Feedback](https://github.com/carbon-design-system/carbon-ai-chat/issues)

## Overview

Use `cds-aichat-shell` to create a flexible chat interface layout with support for headers, messages, input areas, workspaces, history panels, and overlay panels. The shell component provides a structured container that manages the layout and positioning of various chat interface elements.

<Canvas of={ChatShellStories.Default} />

The shell includes support for:

- **Header and footer slots** for custom branding and actions
- **Messages and input areas** with before/after extension points
- **Workspace and history sidebars** with configurable positioning
- **Overlay panels** (`cds-aichat-panel`) with priority management and animations
- **Responsive layouts** with optional frames and rounded corners
- **AI-enabled theming** for enhanced visual styling

### Getting started

Here's a quick example to get you started.

```html
<cds-aichat-shell content-max-width show-frame corner-all="round">
  <div slot="header">Chat Header</div>
  <div slot="messages">Messages Area</div>
  <div slot="input">Input Area</div>
</cds-aichat-shell>
```

### Using panels

Panels can be added to create overlay experiences with different priorities and animations:

```html
<cds-aichat-shell content-max-width show-frame>
  <div slot="header">Chat Header</div>
  <div slot="messages">Messages Area</div>
  <div slot="input">Input Area</div>

  <div slot="panels">
    <cds-aichat-panel
      open
      show-chat-header
      show-frame
      animation-on-open="slide-in-from-bottom"
      animation-on-close="slide-out-to-bottom"
    >
      <div slot="header">Panel Header</div>
      <div slot="body">Panel Content</div>
    </cds-aichat-panel>
  </div>
</cds-aichat-shell>
```

## Slots

`Slots` demonstrates every slot the shell exposes, with checkboxes to toggle each one's visibility and controls for the layout tokens below.

<Canvas of={ChatShellStories.Slots} />

## Sidebar workspace

`SidebarWorkspace` shows the workspace sidebar sliding open alongside the messages area, toggled from a button inside the messages slot.

<Canvas of={ChatShellStories.SidebarWorkspace} />

## Customization

> ** For information about styling slotted content with modifiers (`data-rounded`, `data-stacked`) and CSS custom properties, see [Styling & Modifiers](/docs/introduction-styling-modifiers--overview).**

### Layout tokens

Use CSS custom properties to adjust layout sizing.

```css
cds-aichat-shell {
  --cds-aichat-messages-max-width: 720px;
  --cds-aichat-messages-min-width: 320px;
  --cds-aichat-workspace-min-width: 520px;
  --cds-aichat-history-width: 360px;
  --cds-aichat-card-max-width: 460px;
}
```

| Token                              | Default | Description                                    |
| ---------------------------------- | ------- | ---------------------------------------------- |
| `--cds-aichat-messages-max-width`  | `672px` | Maximum width for message content area         |
| `--cds-aichat-messages-min-width`  | `320px` | Minimum width for message content area         |
| `--cds-aichat-workspace-min-width` | `480px` | Minimum width for workspace panel              |
| `--cds-aichat-history-width`       | `320px` | Width of the history / conversation list panel |
| `--cds-aichat-card-max-width`      | `424px` | Maximum width for card components              |

## Component API

<ArgTypes of="cds-aichat-shell" />

## JS (via import)

```javascript
import "@carbon/ai-chat-components/es/components/chat-shell/index.js";
```

## Feedback

Open an issue at https://github.com/carbon-design-system/carbon-ai-chat/issues to provide feedback or request changes.
